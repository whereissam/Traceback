# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — real keys never get committed
.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code config (hooks, skills, settings)
.claude/

# Python artefacts (modal/)
__pycache__/
*.py[cod]
.venv/

# Astro build cache (landing/)
.astro/

# Chinese working notes — local only, not for the public repo
docs/traceback/PITCH-zh.md
docs/traceback/GUIDE-zh.md

# Playwright MCP scratch output (screenshots, page snapshots, console logs).
# Only loose screenshots dropped at the repo root — never public/ or landing/
# assets, which are real deliverables.
.playwright-mcp/
/*.png
